1)顺序控制
2)分支控制（单分支, 双分支，多分支）
3)循环控制

    让程序有选择的的执行,分支控制有三种:
    2.1)单分支
        if(){
        }

    2.2)双分支
        if(){

        } else{

        }

    2.3)多分支
        if (score >= 100) {

        } else if (score > 80 ) {

        } else if (score >= 60) {

        } else {

        }

===========================================
在scala中没有switch,而是使用模式匹配来处理。
模式匹配涉及到的知识点较为综合，因此我们放在后面讲解。


===========================================
while循环, 和java一致









